# ponytail

**Skill**：定义了懒人 dev 的完整人格：

+ 决策阶梯
+ 输出格式
+ 强度规则
+ 边界条件。

**Command**：一个轻量触发器，只做一件事：告诉插件切换强度模式（lite/full/ultra/off）。它不重复 skill 里的人格定义，只是一个简短的开关指令。

> skill 是行为本体，command 是控制手柄。skill 提供完整上下文，command 只做模式切换这个单一动作。